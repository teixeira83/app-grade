import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
  bodyContainer: {
    padding: 24,
  },
});

const colors = {
  primary: '#c9c9c9',
  secondary: '#ffffff',
};

export { globalStyles, colors };
